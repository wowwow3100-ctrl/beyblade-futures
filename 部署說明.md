# 🔥 陀螺小型期貨 X — 部署指南

## 準備時間：約 15 分鐘

---

## 第一步：安裝工具

### 安裝 Node.js
1. 打開 https://nodejs.org
2. 下載「LTS」版本（左邊那個）
3. 一路「Next」安裝完畢
4. 打開「終端機」（Mac）或「命令提示字元」（Windows），輸入：
   ```
   node -v
   ```
   看到版本號代表成功 ✅

### 安裝 Git
1. 打開 https://git-scm.com
2. 下載安裝，一路「Next」
3. 終端機輸入：
   ```
   git --version
   ```
   看到版本號代表成功 ✅

---

## 第二步：取得 Anthropic API Key

1. 打開 https://console.anthropic.com
2. 登入或註冊帳號
3. 左側選「API Keys」→「Create Key」
4. 複製那串 `sk-ant-...` 的金鑰，**存到記事本備用**

⚠️ 這個 Key 要保密，不能上傳到 GitHub！

---

## 第三步：設定專案

把這個資料夾（`beyblade-futures`）放到你想要的位置，
然後打開終端機，`cd` 進去：

```bash
cd beyblade-futures
npm install
```

等安裝完成（約 1 分鐘）。

### 建立 .env 檔案
在 `beyblade-futures` 資料夾裡，新增一個檔案叫 `.env`：

```
VITE_ANTHROPIC_API_KEY=sk-ant-你的key貼在這裡
```

然後修改 `src/App.jsx`，找到這行：
```js
headers: { "Content-Type": "application/json" },
```
改成：
```js
headers: {
  "Content-Type": "application/json",
  "x-api-key": import.meta.env.VITE_ANTHROPIC_API_KEY,
  "anthropic-version": "2023-06-01",
  "anthropic-dangerous-direct-browser-access": "true",
},
```

### 本機測試
```bash
npm run dev
```
打開瀏覽器 http://localhost:5173 就能看到網頁 🎉

---

## 第四步：上傳到 GitHub

1. 打開 https://github.com → 登入 → 點「New」新增一個 repo
2. repo 名稱填 `beyblade-futures`，設為 Public，按「Create」

3. 回到終端機，在 `beyblade-futures` 資料夾執行：
```bash
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/你的帳號/beyblade-futures.git
git push -u origin main
```

⚠️ 確認 `.gitignore` 裡有 `.env` 這行（保護你的 API Key）

---

## 第五步：部署到 Vercel

1. 打開 https://vercel.com → 用 GitHub 帳號登入
2. 點「Add New Project」→ 選你剛才的 `beyblade-futures` repo
3. Framework Preset 選「Vite」
4. 展開「Environment Variables」，新增：
   - Name: `VITE_ANTHROPIC_API_KEY`
   - Value: `sk-ant-你的key`
5. 按「Deploy」

等約 1 分鐘，你會拿到一個網址如：
```
https://beyblade-futures-你的帳號.vercel.app
```

這個網址就可以分享給任何人！🔥

---

## 之後要更新網頁？

直接修改 `src/App.jsx` 或 `src/App.css`，
然後：
```bash
git add .
git commit -m "更新內容"
git push
```
Vercel 會自動重新部署，約 1 分鐘後生效。

---

## 常見問題

**Q: AI 更新按鈕沒反應？**
A: 確認 API Key 有正確填入 Vercel 的環境變數，且 headers 有加上面的三行。

**Q: 部署成功但畫面空白？**
A: 打開瀏覽器 F12 看 Console 有沒有錯誤訊息。

**Q: API Key 用完怎麼辦？**
A: 去 console.anthropic.com 充值或建立新 Key。

---

有問題直接問 Claude！🥊
