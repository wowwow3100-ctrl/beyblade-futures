# 戰鬥陀螺報價趨勢 — 專案交接文件 v2

## 📌 給下一位 Claude 的快速指引

**使用者叫阿志，問題請直接回答，不要囉嗦。**
**每次給程式碼前先自我檢查（避免漏宣告變數、刪功能沒清乾淨等問題）。**
**改完都打包成 ZIP 給阿志，他解壓覆蓋 → 點 `deploy.bat` 自動部署。**
**`.env` 直接放進 ZIP，不用叫他自己建。**

---

## 專案簡介
給台灣戰鬥陀螺玩家看的報價趨勢網站，顯示熱門款式的二手市場行情、每月漲幅趨勢、蝦皮補貨情報。

- **線上網址：** https://beyblade-futures.vercel.app
- **GitHub：** https://github.com/wowwow3100-ctrl/beyblade-futures
- **本機資料夾：** `C:\Users\asus\Desktop\beyblade-v2`

---

## 技術架構

- 前端：React + Vite (JSX + CSS)
- 部署：Vercel（連接 GitHub，push 後自動部署）
- 後端：Vercel Edge Function（`api/stock.js`，爬蝦皮補貨資訊）
- Analytics：Vercel Analytics（已整合）

---

## 資料夾結構

```
beyblade-v2/
├── src/
│   ├── App.jsx          ← 主程式（所有頁面邏輯 + ALL_BLADES 資料 + BladeIcon）
│   ├── App.css          ← 所有樣式
│   └── main.jsx         ← 入口（含 Vercel Analytics inject）
├── api/
│   └── stock.js         ← Vercel Edge Function，爬蝦皮補貨
├── public/
│   └── blades/          ← 本地圖片放這裡，檔名 = 陀螺 code.png
├── index.html
├── package.json
├── vite.config.js
├── .env                 ← API Key（已包含在 ZIP）
├── .gitignore           ← 包含 .env、node_modules
└── deploy.bat           ← 一鍵部署腳本
```

---

## 部署方式

阿志的環境是 Windows + cmd，所有部署都靠 `deploy.bat`：

1. 解壓 ZIP 覆蓋舊資料夾
2. 點兩下 `deploy.bat`
3. Vercel 約 1 分鐘自動更新

不要叫他用 PowerShell（會擋指令碼），用「命令提示字元 cmd」。

---

## 網站功能總覽

### 📈 報價趨勢（首頁）
- 本週漲幅榜（紅色調）— 3月→4月 單月漲幅前 5 名
- 半年來漲幅王（金色調）— 11月→4月 累積漲幅前 5 名（高到低）
- 兩個榜單都可點擊跳到該陀螺詳情
- 搜尋功能 + 追蹤卡片（含 Sparkline）
- 點卡片展開：每月價格表、官方定價、溢價

### 🏪 補貨情報
- 呼叫 `/api/stock` 爬蝦皮現貨
- 注意：localhost 跑不起來，需部署到 Vercel

### 📋 陀螺清單（48款）
- BX 21款 / UX 13款 / CX 8款 / 限定+配件 6款
- 系列過濾、全選/全不選、每系列獨立全選

---

## 陀螺資料結構（ALL_BLADES）

```js
{
  id: 1,
  series: "UX",                       // BX / UX / CX / BXG
  code: "UX-15",                      // 型號（也是圖檔名）
  name: "鯊魚組",
  fullName: "Shark Scale 4-50UF",
  type: "攻擊",                       // 攻擊/耐久/平衡/防禦/限定/配件/套裝
  emoji: "🦈",                        // 圖片載入失敗時 fallback
  officialPrice: 700,
  history: [1800,1900,2000,2200,2400,2500],  // 11→12→1→2→3→4 月
  hot: true,                          // 可選
  img: "https://..."                  // 可選，外部圖片URL（優先用）
}
```

---

## 🖼️ 圖片支援機制

`BladeIcon` 元件邏輯：
1. 優先用 `blade.img`（外部URL）
2. 其次用 `/blades/{code}.png`（本地檔）
3. 都失敗就 fallback 到 `blade.emoji`

**目前進度：** 0/48 款，所有陀螺都還在用 emoji。

### 補圖建議流程（給下一位 Claude）

阿志想要慢慢補，每次開新對話處理 5-10 款：

1. 用 `web_search` 找官方圖（搜尋詞：`"陀螺型號" Beyblade Wiki Fandom`）
2. 優先選 Fandom CDN `static.wikia.nocookie.net` 的圖（穩定且公開）
3. 確認 URL 是直接連到圖檔（.png/.jpg）
4. 寫進 `ALL_BLADES` 對應陀螺的 `img` 欄位
5. 每處理 5-10 款就打包 ZIP，避免對話塞爆

---

## 已知問題 / 待做

- [ ] 48 款陀螺的官方圖（分批處理中）
- [ ] 比賽資訊分頁（計畫手動填 JSON）
- [ ] 價格資料定期更新（目前手動）

## 已完成

- [x] 改名「戰鬥陀螺報價趨勢」、移除 AI 更新按鈕
- [x] 手機優化（max-width 640px、safe area）
- [x] 完整 48 款陀螺清單 + 全選 + 系列過濾
- [x] 本週/半年雙漲幅榜
- [x] 蝦皮補貨情報（Edge Function）
- [x] Vercel Analytics（真實瀏覽量）
- [x] BladeIcon 元件（圖片+emoji fallback）
- [x] deploy.bat 一鍵部署
- [x] 移除獲利試算

---

## 環境變數（.env，已包在 ZIP）

```
VITE_ANTHROPIC_API_KEY=sk-ant-api03-...（目前未使用）
```

---

## 帳號資訊

- GitHub：wowwow3100-ctrl
- Email：wowwow3100@gmail.com
- Vercel：用 GitHub 登入

---

## 設計風格

- 動漫戰鬥感：深色背景、火焰粒子動畫、Bangers 字型
- 主色：火焰橘 #ff4400、金黃 #ffaa00
- 手機優先，最大寬度 640px

---

## ⚠️ 開發注意事項

1. **每次改完先檢查**：變數有沒有宣告/刪除完整、JSX 括號、CSS class
2. **不要用中文 batch 檔**：Windows cmd 編碼會亂碼
3. **deploy.bat 必須有 `cd /d "%~dp0"`**：避免被當成 System32 執行
4. **localhost 上補貨情報不會動**：正常的，只有 Vercel 部署版才能用 API
5. **改 App.jsx 後一定要重新 grep 檢查未定義變數**：曾經發生過刪獲利試算忘記刪 `g` 變數的事故

---

*文件最後更新：2026年5月1日 v2*
